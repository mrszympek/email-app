<?php

// connect to database
$conn = mysqli_connect('localhost', 'root', 'root', 'email');

// Create new table
/*
$db = "CREATE TABLE `email_list` (id int PRIMARY KEY AUTO_INCREMENT, first_name VARCHAR(20), last_name VARCHAR(20), email VARCHAR(60))";
$query = mysqli_query($conn, $db);
*/